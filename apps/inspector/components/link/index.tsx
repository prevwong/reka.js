import { styled } from '@app/styles';

export const Link = styled('a', {
  display: 'inline-block',
  cursor: 'pointer',
  fontWeight: '500',
  svg: {
    ml: '$1',
    display: 'inline-block',
  },
});
